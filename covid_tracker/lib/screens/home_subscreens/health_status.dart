import 'package:flutter/material.dart';

class HealthStatus extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('YOUR HEALTH STATUS'),
      ),
    );
  }
}
